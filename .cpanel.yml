---
deployment:
  tasks:
    - export DEPLOYPATH=/home/thebertg/cam.thebizportwebs.online/

    - /bin/mkdir -p $DEPLOYPATH

    - /bin/cp -R * $DEPLOYPATH

    - cd $DEPLOYPATH

    - npm install

    - npm run build

    - touch tmp/restart.txt